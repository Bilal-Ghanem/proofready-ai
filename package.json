{
  "name": "proofready-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check core.js && node --test",
    "smoke": "node tests/browser-smoke.mjs"
  }
}
